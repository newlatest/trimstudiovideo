   [build]
     command = "npm install && npm run vite:build"
     publish = "dist"

   [build.environment]
     NODE_VERSION = "20"

   [[redirects]]
     from = "/api/*"
     to = "https://api.muapi.ai/api/:splat"
     status = 200
     force = true
